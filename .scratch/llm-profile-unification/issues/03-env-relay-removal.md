Claimed-at: 2026-08-30T05:33:15.468Z
# 03 - 环境变量中继拆除与旧客户端显式传参

**构建什么.** 环境变量中继拆除与旧客户端显式传参

**阻塞于.** 06

**状态.** resolved


## Answer

共识落定（2026-08-30 grilling 三轮，九条决策 + 两个明示行为变化，全部供 to-spec 坍缩；缓存落地按 06 票 research/06-gateway-disk-cache-design.md §五清单先行——缓存落地前 client.py 不退役）。(1) 消费点参数面：断句/优化/压缩/语义修复构造签名一律 profile: LLMModelProfile + gateway: Optional[LLMGateway] = None（None 时 LLMGateway() 惰性构造），照 llm_translator.py:37-38,55-56 / check_llm.py:180,218 / orchestrator.py:457 三处先例；门面对象否决（resolver 已是 02 定的单一入口，再包一层是重复抽象）、模块级单例否决（gateway 持 adapter 缓存与 per-profile 信号量，全局单例测试隔离差）。(2) gateway 所有权：per-consumer 惰性，不做任务级共享——三阶段（断句/优化/翻译）顺序执行同一时刻只有一个阶段发请求，阶段内双层并发（消费点线程池 + gateway 信号量）维持 02 决策 11。(3) SubtitleConfig 六标量终局：base_url/api_key/llm_model + utility_llm_base_url/utility_llm_api_key/utility_llm_model 六个全删（entities.py:727-734），新增 utility_llm_profile: Optional[LLMModelProfile]（主翻译侧继续用已有 main_llm_profile/review_llm_profile）；task_factory 装配段直接塞 profile 不展开标量（修正 04 票的展开过渡态，其时已注明终局归本票）；llm_translator.py:235 的 call_llm fallback 分支与 RetranslateThread :558-565 env 写入分支一并删除；行为变化一：「OPENAI 翻译但无 profile」从静默写 env 继续跑变 fail-fast 报错指引进 profile 体系（与 02 决策 7 精神一致）。(4) timeout 语义：LLMRequest 加 timeout: Optional[float] = None，adapter 发请求时 request.timeout or self.timeout 覆盖构造默认 120s（adapters.py:647 三处使用点）；断句 30s（split_by_llm.py:16）、语义修复 60s（semantic.py:35）保真；行为变化二：优化/压缩现状不传（SDK 默认 600s）迁移后落 120s——gateway 4 次重试兜底（最坏 ≈480s+退避），翻译路径 120s 实证够用，工具角色对齐翻译语义。(5) 结构化输出升格：semantic 两 schema（_parse_rewrite_response 期待的 {window_id, segments:[{cue_id,text}]} 与 _parse_review_response 期待的 {window_id, decision, changed_facts, explanation}）与 rewriter schema（{items:[{index,text}]}）形式化成 JSON Schema 走 LLMRequest.response_schema——adapter 按 dialect 自动分档（adapters.py:420-449：openai 系 json_schema / deepseek 系 tool / generic json_object），generic 档与现状 response_format={"type":"json_object"} 完全等价零回归，档位越高收益越大；与增强翻译（orchestrator 已用 response_schema）拉齐标准面；semantic.py:208-219 _response_content 多形态解析简化为纯 str。(6) cli/commands/transcribe.py:61-68 的 OPENAI_* env 写入顺带删——whisper_api.py 显式收参（:64），全程零读取者，纯 vestigial 死写入。(7) normalize_base_url 搬 core/llm/check_llm.py（探测函数的家，get_available_models 也住在那），check_llm.py:263,281 / check_whisper.py:67 / whisper_api.py:53 / mimo_asr.py:229 四个存活消费者各改一行 import；client.py 整文件退役（连带 get_llm_client/call_llm/_call_llm_api/_sanitized_openai_error/_sanitize_legacy_extra_body/before_sleep_log 全删）。(8) check_llm_connection 三标量旧入口删除——04 删旧服务页 + 02 决策 7 预检改本地校验后零生产消费者；profile 版 check_model_profile_connection 留给 profile 编辑器探测按钮；conftest.py:243 patch 点一并删。(9) 拆除总面：env 写入 5 处全删（subtitle_thread.py:149-150,564-565 / cli subtitle.py:223,225 / postprocess.py:172,174 / transcribe.py:66,68）；request_logger.py legacy 三函数（create_logging_http_client:332 / discard_pending_legacy_request:343 / log_llm_response:354，唯一消费者即 client.py）随之删，_legacy_base_entry:248 连带消亡——llm_requests.jsonl 的 legacy 标签从源头消失（Destination 兑现）；测试缝从「setenv 假值 + patch 5 处 call_llm 符号 + patch check_llm_connection」（conftest.py:225-245）换成 fake gateway 注入；tests/test_llm/test_client_privacy.py 三用例（测的正是 client._call_llm_api 温度剥离与错误脱敏）随 client.py 整体退役删除；cache.py get_llm_cache 在 src 零消费者后连同 conftest:35 引用删（memoize 本体保留，06 票已定）。DubbingConfig 三元组换 Optional[LLMModelProfile] + rewriter 迁 gateway 在本票拆除面内（02 决策 10），CLI 侧装配（dub.py 从哪读 profile、TOML 键面）归进行中的 05 票。行为变化三处已向用户明示并确认：优化/压缩 timeout 600s→120s 收紧、无 profile 翻译 fail-fast、generic dialect 下 semantic/rewriter 结构化从手工校验升 schema 档（json_object 档行为等价）。

> 未提升到 ADR：九条决策全是 ADR-0014 已定架构（profile 统一、client.py/env 中继退役、五消费点迁 gateway）之下的执行层决策，与 04/06 两票同层：逐条可逆（六标量加回、check_llm_connection 恢复薄包装、timeout 字段移除即回原状），不构成「没上下文会显得意外」（目的地直接表达），无体系级真实权衡（备选都是同架构内的实现取舍）。体系级不可逆决策已由 ADR-0014 承载。
