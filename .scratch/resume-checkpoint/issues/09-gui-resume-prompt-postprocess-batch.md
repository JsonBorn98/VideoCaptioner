Autopilot: true
Claimed-at: 2026-09-17T07:37:22.178Z
# 09 - GUI 恢复提示：后处理，以及批量任务默认继续

**构建什么.** 作为 GUI 用户，独立后处理任务和完整 workflow 编排的单个任务在后处理模块开始时发现匹配的恢复检查点，页面弹出与翻译相同的恢复提示（复用 08 的对话框），我选「从检查点继续」或「从头开始」；作为批量任务用户，批量任务不弹提示、默认继续，任务行状态标注「已从恢复检查点继续」，半夜的批量不会卡在弹窗上。

后处理线程新增「需要恢复决定」信号与提交方法（与 08 相同的阻塞等待模式），接到 06 的恢复决定回调；漂移清单来自 07。独立后处理页面与主页编排单任务（后处理阶段）走提示路径。批量任务线程对翻译与后处理两个模块都不接回调（默认继续），模块从检查点继续时把标注写进任务行状态。没有匹配检查点时不弹、不标注。遵守 ADR-0022 与功能页面边界。

**阻塞于.** 06, 07, 08

**状态.** resolved

- [x] 有匹配检查点时后处理线程发出「需要恢复决定」信号并阻塞至提交，提交后按选择运行（offscreen Qt 测试）
- [x] 独立后处理任务与完整 workflow 编排单任务的后处理阶段都走提示路径；无匹配检查点时不弹
- [x] 批量任务线程对翻译与后处理都不发信号、默认继续；任一模块从检查点继续时任务行状态带「已从恢复检查点继续」标注，未续跑的行不带
- [x] 复用 08 的对话框组件，不复制第二份
- [x] 既有后处理线程取消门禁、批量线程、流水线线程测试全部通过
