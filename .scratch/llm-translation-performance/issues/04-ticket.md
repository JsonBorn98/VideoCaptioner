Claimed-at: 2026-09-01T13:51:00.546Z
# 04 - 批并发执行模块收编正式翻译批

**构建什么.** 增强翻译编排器内部出现批并发执行接口：入参为批次序列、单批执行函数、并发度、取消令牌与完成回调；滑动窗口并发、取消传播与自适应首批预热收进实现。先让已有的正式翻译批走它——失败降级语义与现在一致（机械重试后整任务失败）；取消时在途批次放弃且已完成结果保留；批数 ≤ 并发度时跳过首批串行预热，批数充足时保留预热以便供应商前缀缓存。这是后面三阶段并发化的预重构。

**阻塞于.** 01

**状态.** resolved

- [ ] 正式翻译批穿过批并发执行；最大同时在途数不超过并发度
- [ ] 乱序完成时结果按字幕 ID 正确合并
- [ ] 取消时在途批次放弃且已完成批次保留
- [ ] 批数 ≤ 并发度时跳过首批预热；批数充足时保留首批串行预热
- [ ] 单批失败降级（机械重试后整任务失败）语义与现在一致
