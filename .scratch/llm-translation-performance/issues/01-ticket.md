Claimed-at: 2026-09-01T06:06:12.202Z
# 01 - 并发控制面：任务级并发请求数直通

**构建什么.** 任务级「并发请求数」旋钮真实生效。LLM 网关按模型配置方案维护的并发闸大小直通该值；方案里的并发上限默认不夹，仅显式设置时作为保护夹钳。旧配置遗留的默认并发上限值在升级时清除。GUI 与 CLI 默认统一为 10，旋钮文案改为「并发请求数」。主翻译与高级校对绑定不同模型配置方案时各自吃满各自的并发额度。429/服务端错误继续由既有退避重试（遵循 Retry-After）兜底。现有正式翻译批的并发路径即可验证：调大旋钮真的变快，不再被方案里看不见的默认值压住。

**阻塞于.** 无，可立即开始

**状态.** resolved

- [ ] 假适配器工厂：并发闸大小直通任务级并发请求数
- [ ] 可选夹钳默认不夹；显式设置时作为该方案保护上限参与钳制
- [ ] 旧配置遗留的默认并发上限值迁移后清除为不夹
- [ ] GUI 与 CLI 并发请求数默认均为 10；GUI 旋钮文案为「并发请求数」
- [ ] 主翻译与高级校对绑定不同模型配置方案时各自吃满各自额度、互不挤占
