Claimed-at: 2026-09-02T02:20:29.505Z
# 06 - 全文分析、术语候选、审计批走批并发执行

**构建什么.** 原先完全串行的全文分析窗口、术语候选、审计批三个阶段，无依赖批次按并发度并发发出。术语候选内「主建议 → 校对裁决」仍串行；手动术语确认仍在全部候选解析后一次性弹出；审计输出限额拆批与手动审计确认语义不变。请求内容语义零变更：稳定前缀、块级术语注入与 brief 结构全部不变。这是墙钟缩短的主路径。

**阻塞于.** 04

**状态.** resolved

- [ ] 四阶段请求的最大同时在途数不超过并发度
- [ ] 术语候选内主建议→校对裁决保持先后依赖
- [ ] 手动术语确认仍在全部候选解析后一次触发
- [ ] 审计输出限额拆批路径行为不变；手动审计确认语义不变
- [ ] 取消时在途批次放弃且已完成结果保留
