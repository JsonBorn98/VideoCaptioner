Claimed-at: 2026-09-01T08:02:19.735Z
# 02 - 翻译批处理上限 500，输出预留与请求超时随批缩放

**构建什么.** 翻译批处理上限最高可调到 500（默认仍 10；正式翻译批与审计批共用；CLI --batch-size 同样接受；single 模式范围不动）。单批实际携带的字幕主体数仍由 token 规划在工作上下文与输出预留约束下向下收缩。输出预留由固定封顶改为主体输入估算 × 输出比（翻译约 1.2、审计约 1.3）+ 固定 JSON 结构开销，仍钳制在工作上下文与用户配置的请求输出上限之内；大批不再被固定封顶压小后截断。请求超时由固定 120 秒改为基线 + 请求输出上限 × 每 token 时间系数，三种传输接口一致采用；小批保持快速失败，长生成不被固定超时误杀。自动档输出上限升档梯次不动。

**阻塞于.** 无，可立即开始

**状态.** resolved

- [ ] 增强翻译批配置取值范围 1-500，默认 10；正式翻译批与审计批共用该上限
- [ ] CLI --batch-size 接受到 500，默认 10；single 模式批配置范围不变
- [ ] 输出预留按主体输入比例缩放后，批规划产出的主体数不再被固定封顶压小
- [ ] 三种传输接口的请求超时随请求输出上限延长；小批保持基线超时快速失败
- [ ] UI offscreen：批处理上限控件取值范围 1-500 且默认 10
