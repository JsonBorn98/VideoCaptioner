Autopilot: true
Claimed-at: 2026-09-05T13:16:16.323Z
# 05: 实现批量修复、一对多结果与局部回退

**What to build:** 让核心入口执行批量观看约束修复，支持一个输入字幕段产生多个输出段。每轮把候选拼回完整字幕并重新验收，只重试剩余问题；首次提交不计入重试，首次失败后最多 4 次业务修复重试，耗尽时只回退失败区域并继续完整 workflow 下游。

**Blocked by:** 04

**Status:** resolved

- [ ] 修复响应显式绑定问题 ID、输出段序号、原文片段和译文片段，不能按数组位置推断归属。
- [ ] 一个输入段可以产生多个输出段，原文片段按顺序拼接后与输入原文等价。
- [ ] 输出时间由后处理确定性规则和可信时间证据决定，模型不能直接破坏时间轴。
- [ ] 每轮候选拼回完整字幕后重新执行结构、原文等价、语义、绝对长度和阅读速度验收。
- [ ] 已通过问题不再进入后续请求；重复候选、重复指纹或无法改善的候选停止并进入报告。
- [ ] 首次提交属于正常流程，首次失败后最多执行 4 次业务修复重试，网络级重试单独计数。
- [ ] 业务重试耗尽时恢复该问题区域的初版未拆分快照，保留其他成功区域并标记未解决。
- [ ] 局部未解决不阻断完整 workflow 下游；模块级失败仍阻断并回退整个活动字幕。
## Comments
> *This was generated by AI during triage.*

## 双轴审查结果（2026-09-05，commit 4d9a9ec → e025065/b75097c）

**编码规范轴**：无硬违规。5 条坏味判断题，4 条已修（e025065：char_count 抽到 viewing.py 共享、runner 单 with 块、修复警告并入 task warnings、函数内 import 提顶、测试死代码清理），1 条裁决保留（片段 dict 流经内部函数——镜像 json_repair 的 JSON 线格式，加转换层反向收益）。

**Spec 轴**：8 条验收逐项核对，6 条无误。2 条裁决记录如下（人复核点）：

1. **D24 后半「时间证据允许的相邻安全空隙调整边界」未接入**——保守读法裁决：D24 用许可式语言（「只允许」），未授权任何边界调整时该约束空洞成立；`_allocate_times` 严格在原 cue 内分配，`min_duration_ms` 下限由 `_max_fragments` 构造保证（`duration // min_duration_ms` 封顶片段数）。证据驱动的间隙借用是票 06（翻译执行快照接线）或后续增强的候选，不阻塞本票。
2. **D13 字面「后处理开始时保存的初版输入快照」vs 实现取修复入口（确定性阶段后）快照**——裁决保留现行为：确定性产物是已完整验收的最后状态（D10 意图「不取某次未完整验收的中间结果」）；回退到后处理入口会复活用户显式开启的确定性修正（占位符清理/间隙闭合）。依据已写进 repair.py 模块 docstring。

**口径说明**：被拒候选不记指纹（验收通过才记），重复失败候选由 1+4 业务重试计数兜底，回退原因记「业务修复重试耗尽」而非「重复候选」——「停止并进入报告」字面仍满足，测试已如实固定此选择。

**测试**：tests/test_postprocess/test_repair_execution.py 21 个测试全过；全量非集成 1171 passed（1 个 UI 测试偶发 Qt 子进程崩溃已单独重跑通过，记入 shoals）。Ruff/Pyright 全绿。
