Autopilot: true
Claimed-at: 2026-09-11T05:12:18.697Z
# 02 - 冻结修复轮次并确定性归并

**构建什么.** 让完整后处理任务中同一问题修复轮次读取统一修复轮次快照，修复主体与边界上下文不随邻批完成而改变；候选按固定段序归并，一对多拆分、问题追踪与区域回退不再依赖请求完成顺序。本票是并发前的结构准备，但须贯穿现有任务入口并产生可验证的字幕与报告，不交付孤立数据结构。

依据本 feature 规格中“修复轮次快照与候选归并”、P08 和 ADR-0021。先在现有串行执行下分离载荷构建、候选计算和统一归并，保留对外入口与既有角色流程；工作单元不直接改共享字幕或重试簿记。高级校对读取本轮底稿叠加对应主修复候选的明确版本，不读取邻批刚完成的修改。

复用完整后处理任务入口与受控模型响应进行测试，不逐层模拟内部函数。使用01冻结的基准及本地开销门槛；本票不引入并发调度、新恢复机制或真实模型调用。原文保护、必要校对、质量验收和下游门禁不得削弱。

**阻塞于.** 01

**状态.** resolved

- [x] 完整任务对同轮各批构造的主体和边界上下文来自统一修复轮次快照；相邻批的拆分、回退或校对不改变其他请求载荷，下一轮才读取归并后的字幕。
- [x] 主修复候选和高级校对结果具有稳定的问题身份、主体范围及版本绑定；同轮共享边界上下文不成为修改目标，重叠或相邻主体沿用既有合并规则。
- [x] 通过同一响应集合的不同候选完成/提交顺序，证明最终字幕、问题验收、计数与区域回退一致；断言不要求真实模型两次生成逐字相同。
- [x] 覆盖高低段序一对多拆分、多问题对应同段、主体重叠、邻域共享上下文、局部失败及回退，归并后索引、原文顺序和时间安全保持正确。
- [x] 重复候选、无改善、业务修复重试耗尽及全片重检行为保持；成功问题不因其他问题进入下一轮而重新处理，未解决问题不被记为通过。
- [x] 完整任务结果、单独成果保存及下游门禁回归通过；内部候选不提前交付，停止后的部分成果不被发布，输入文件与原始资产不变。
- [x] 运行01对应离线基准并满足已冻结的本地性能容忍门槛；使用项目uv测试命令，报告实际测试结果，不发真实模型请求。

## Comments

2026-09-11 交付：commit `9b2a6ba`（已提交本地 `master`，未 push）。双轴审查 4 项实质发现当场修完并过一轮修复回审（3 条事实断言逐项以测试/探针验证）。

- 实现：`repair.py` 每轮冻结 `round_snapshot = state.as_data()`；`_build_payload`/候选验收只读快照；`_review_pass` 上下文以快照为底叠加本主体候选，窗口以主体输入段数（`len(replacements)`）为界。D27 守卫 / 业务重试记账 / 区域回退不变。未引入并发调度（属票 04/05）。
- 新增 `tests/test_postprocess/test_round_snapshot_merge.py` 8 测试：统一快照载荷、校对不读邻批归并、响应三种排列确定性、多问题同段身份与回退记账、局部失败隔离、重复守卫。
- 回归：后处理目录 195 passed；全量离线 `QT_QPA_PLATFORM=offscreen uv run --no-sync pytest -m 'not integration and not llm'` → 1480 passed / 5 skipped / 61 deselected；ruff、pyright（repair.py 0/0）通过。全量跑出过的 2 个 `tests/test_ui` 失败经干净基线复现确认为 Qt offscreen 环境抖动（0xC0000005 字体目录问题），与本票无关，复跑全绿。
- 01 冻结基准复跑（gitignored `benchmark-output/ticket02-*`）：local-long median 2.207s（修复后复测 1.966s）≤ 门槛 2.5225s，跨度 11.6% < 15% 噪声规则；independent/mixed/partial-failure 请求数、覆盖、回退、输出段数与冻结基线一致。slow-main median 2.446s（vs 基线 2.402s）——本票为结构准备票，×0.75 并发改善门槛属票 04/05。
- 修复回审断言：复校上下文窗口坐标（探针验证 `[0,1,2]` 以主体输入段数为界）；`_context_entry` 共享形状 / 死代码 `index<0` 删除 / 术语「本轮底稿」→「轮次快照」统一（inspect 验证）；多问题同段身份真实性（扫描实测产生 `length:original:0` + `length:translated:0`）。
- 审查遗留（不阻塞）：`_review_pass` 13 参数 Data Clumps（审查者同判暂不动）；spec 轴「基准复跑无 diff 产物」属交付记录性质（基准输出在 gitignored 目录，文档实测表不更新——01 票已冻结，后续票对照门槛验收）。`.scratch/` 保持本机隔离，未提交或外传。
