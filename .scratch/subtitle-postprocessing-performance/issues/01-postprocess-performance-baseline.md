Autopilot: true
Claimed-at: 2026-09-11T04:11:17.829Z
# 01 - 建立后处理性能基准与验收矩阵

**构建什么.** 构建什么：为“字幕后处理性能治理：有界并发、可观察执行与及时停止”规格建立第一条可复跑的完整任务基线，让维护者能区分本地处理、主修复、高级校对、排队、退避与任务墙钟，并在任何优化开始前固定量化验收矩阵。本票不实现并发提速、不修改用户模型配置、不发真实模型请求。

使用已确认的完整后处理任务入口作为主要测试接缝，以可控制响应和延迟的假网关/本地服务穿过真实规划、修复、校对、归并和结果交付。保留现有行为作为测量对象，基准所需的轻量观测不得改变修复语义。公共测试使用合成或去敏长字幕；DHH 初版仅作为本地可选样本，基准输出隔离，不能覆盖输入或既有过程资产。

本票同时产出后续优化票统一引用的基线标识、可执行命令及冻结验收矩阵。矩阵记录真实测得的数值、测量条件、容忍噪声与目标依据，不编造整片完成时限。后续票不得在看到优化结果后降低门槛。

遵守本轮 P01–P09、完整任务测试入口确认，以及现有并发控制、容量/超时缩放、观看约束职责和冻结上下文 ADR。历史4.4小时静默仍未归因；跨重启请求汇总不是单次完整任务耗时，缓存命中不是模型吞吐。

**阻塞于.** 无，可立即开始

**状态.** resolved

- [x] 存在使用项目 uv 工具链运行的离线基准入口，从完整后处理任务入口贯穿真实业务路径；常规运行不需要 API key、不访问外部 provider，DHH 文件不存在时仍能运行公共基准。
- [x] 固定并记录输入指纹/规模、后处理配置、角色请求参数、代码版本、执行环境、缓存冷热状态、并发配置及受控响应；基准运行不改写初版文件、用户配置或既有过程资产。
- [x] 至少覆盖长字幕本地处理、足够多独立主体、主修复慢/校对快、多主体与一对多输出、局部失败、排队/网络/退避取消探针；探针可以揭示现状缺陷，但必须有有界清理，不能遗留挂起任务。
- [x] 分别报告主修复/校对逻辑请求数与真实尝试数、主体/问题/字幕段计数、容量估计、请求耗时分布、实际在途数、本地阶段耗时、排队/退避和完整任务墙钟；跨重启与冷热缓存不能混算。
- [x] 在后续优化开始前落成版本化验收矩阵：给出可复跑命令、重复次数、实测基线、噪声范围及明确的并发改善、校对请求数量、各等待阶段停止响应、界面刷新和本地性能回退门槛；每个门槛说明依据和适用条件。
- [x] 基准正确性由确定性断言保障：原文保护、任务输出与下游门禁可观察，测试不通过跳过必要修复或质量检查制造提速；区分预期暴露的现状失败与基准基础设施自身失败。
- [x] 交付记录说明本票没有执行真实模型质量/性能验证，没有解释历史静默空档；后续真实小样本仍需单独确认范围和费用上限。

## Comments

2026-09-11 交付：commit `a2b9670` 已从 `test/postprocess-performance-baseline` fast-forward 合并到本地 `master`，工作区干净，未push。仅交付本票；没有实现并发优化或关闭后续优化/交付票。

- 入口：`uv run --no-sync python -m scripts.postprocess_benchmark --output benchmark-output/NEW_GROUP --repeats 5`。
- 冻结基线与门槛：`docs/dev/postprocess-performance-baseline.md`、安全合成数据 `docs/dev/postprocess-performance-baseline-v1.json`。基线`postprocess-v1`，矩阵`postprocess-acceptance-v1`；原始日志/资产保留在gitignored目录。
- 五次矩阵/三类真实loopback等待探针/Qt取消及清理均完成；local-long原组噪声19.57%整体剔出有效统计但保留证据，整组重采5.10%有效。slow-main median2.401658300s、local-long2.193482600s，未来对应上限1.801243725s/2.522504990s。
- 现状停止median：queue1.360728s、network5.085254s、backoff0.900213s、ui1.202910s；仍未满足未来0.30s门槛，作为基线缺口保留，不冒称已修复。
- 最终离线pytest：1472 passed、5 skipped、61 deselected、1 warning，254.98s，含本票18测试。ruff通过；全项目pyright0 errors/20既有warnings，新增脚本单查0 errors/6 warnings；双轴审查与单轮修复回审记录在本地review文件。
- 未发真实provider请求、不读取DHH正文、不改用户配置，源码与uv.lock指纹已固定。未解释历史4.4小时静默、未验证实际widget；后续真实小样本另确认规模和费用。缓慢分段/5xx留给后续传输取消票补覆盖。
- `.scratch/`保持本机隔离，未提交或外传。
