Claimed-at: 2026-09-11T09:26:34.551Z
# 04 - 主修复受控并发与配置贯通

**构建什么.** 让用户设置的并发请求数在独立GUI、编排后处理及CLI实际生效：容量合法且互不依赖的主修复批次能重叠发出，返回后按02建立的确定性规则归并；不因最慢批次阻塞其他无依赖网络工作，也不让大工程产生无界排队。

依据本 feature 规格“有界并发与入口配置”、ADR-0018和ADR-0021，复用现有批并发执行及每profile保护闸，避免重复旋钮、嵌套上限相乘或全应用调度重构。完整workflow沿用任务冻结设置，独立入口从现有执行配置冻结实际值；无配置时采用权威默认值并留下可观察的有效值，不加跨页快捷设置。

本票仍保持现有高级校对粒度与必要覆盖，05再做校对批量化；主修复到其校对的因果顺序必须正确。06独立增强真实传输停止，本票先保留协作取消、结果隔离和原有失败语义，不声称已具备完整在途取消。实际主请求并发从完整任务入口用受控屏障观察，并对照01冻结矩阵验证吞吐。

**阻塞于.** 02, 03

**状态.** resolved

- [ ] 从完整后处理任务入口证明：有足够独立批且有效额度大于1时，多个主修复请求真实重叠；并发为1时仍正确运行，不通过暖缓存伪造并发。
- [ ] GUI独立、编排、批量任务及CLI的现有并发设置传递到实际执行，任务开始后冻结；不再悄悄退回隐藏网关默认值，不改变用户配置或新增重复旋钮。
- [ ] 同profile请求共享有效保护上限，显式夹钳生效；不同profile遵守既有独立额度语义，主修复/校对或重试的嵌套调度不使上限相乘。
- [ ] 任务提交、在途与候选缓冲均有界，慢批不使其他无依赖请求串行等待；少量批次不会因无收益的固定首批预热被强制串行。
- [ ] 任意响应完成顺序得到02定义的相同字幕、报告与回退；保留03容量校验、必要高级校对、问题身份、原文保护和既有业务/传输重试口径。
- [ ] 协作取消后不继续调度新工作或应用迟到候选，不误用上游翻译的部分成果交付行为；06尚未完成时准确注明真实在途/退避取消的剩余边界。
- [ ] 执行01冻结的并发改善基准，报告有效并发、最大在途、真实请求数及墙钟；本地开销不超门槛，测试不依赖易抖动短sleep或真实provider。
