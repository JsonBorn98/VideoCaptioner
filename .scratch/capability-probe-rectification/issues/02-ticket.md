Claimed-at: 2026-08-31T12:40:16.813Z
# 02 - 输出上限探查按钮：解析 → 建议值 → 验证重试 → 条件填入

**构建什么.** 模型方案编辑器请求输出上限配置行旁新增「输出上限探查」按钮：发起前费用提示（可能发出探查与验证重试两次请求）；以工作上下文-1 发起探查请求 → 从被拒 400 错误解析模型输出上限 → 建议值 = min(解析值, 工作上下文-1) → 用建议值做一次验证重试 → 重试通过才允许填入；仅当已配置的请求输出上限超出模型输出上限时把建议值自动填入配置项（低于时不抬升），自动档只展示不填入；填入不自动保存方案；探查未被 400 拒绝时报「模型输出上限不低于探查值」且不改动配置；解析不出时透传原始错误、不产生建议值（ADR 0017）。沿用探针线程模式（后台线程 + 信号回填），探查中禁用按钮。

**阻塞于.** 01

**状态.** resolved

- [x] 假网关测试：探查请求以工作上下文-1 作为请求输出上限发起；验证重试恰好一次且用建议值；重试通过才允许填入
- [x] 建议值折算 min(解析值, 工作上下文-1)
- [x] 已配置且超出模型输出上限 → 建议值自动填入；低于时不抬升；自动档只展示不填入
- [x] 探查未被 400 拒绝、解析不出数值两个边界：相应结论与配置不动 / 原始错误透传
- [x] 填入不自动保存方案；发起前费用提示；探查中按钮禁用
- [x] UI offscreen 测试：填入 / 不抬升 / 只展示
