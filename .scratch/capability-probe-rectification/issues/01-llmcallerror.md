Claimed-at: 2026-08-31T11:54:42.704Z
# 01 - LLMCallError 携带模型输出上限：三种接口格式的输出侧超限解析

**构建什么.** 注入真实形状的 400 错误体时，OpenAI 兼容、Anthropic Messages、Gemini 三种接口格式的适配器都能解析出模型输出上限，作为结构化字段挂到 LLMCallError 上，并用与输入侧超限（CONTEXT_LIMIT）区分的错误类别标记；解析遵循输入侧超限标记的既有先例，解析不出数值时错误原样透传、绝不猜测。此后 LLMCallError 成为模型输出上限事实的唯一携带通道：能力测试的「需调整」判定与输出上限探查的解析都只读这个结构化字段，不做错误文本二次匹配。

**阻塞于.** 无，可立即开始

**状态.** resolved

- [ ] 三种接口格式各有注入 session/client 的测试：真实形状的 400 错误体 → 错误携带解析出的模型输出上限
- [ ] 输出侧超限的错误类别与输入侧超限（CONTEXT_LIMIT）可区分
- [ ] 400 错误体解析不出数值时不猜测，错误原样透传
- [ ] 敏感错误体内容不进异常文本（脱敏）
