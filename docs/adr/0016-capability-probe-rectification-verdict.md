---
status: accepted
---

# 能力测试结论报「需调整」而非「通过」的三态语义

能力测试测的是用户的配置而非模型能力，验证重试归属输出上限探查按钮，因此能力测试不做降值重试；撞到模型输出上限的 400 时结论报「需调整」，由 LLMCallError 携带的输出上限提示驱动，展示模型输出上限与请求输出上限的对比，绝不因「换个值就能过」而报「通过」——原配置在真实任务中必然 400，绿灯会误导用户。替代方案「验证通过即报通过」因绿灯误导被否决。
