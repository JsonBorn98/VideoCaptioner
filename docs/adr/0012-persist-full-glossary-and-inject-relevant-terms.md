# Persist the full glossary and inject relevant terms

LLM 上下文翻译将全部用户确认结果保存为任务级权威术语表，但每个字幕块只注入与当前块相关的精确子集。稳定缓存前缀包含术语表版本而不包含整张表，块级术语位于缓存断点之后；隐式缓存供应商仍可复用其之前的最长公共前缀。该方案牺牲“每次请求携带完全相同整张术语表”的简单性，换取可控上下文和长视频扩展能力；完整表始终保存在任务数据中，不能被生成式摘要或静默裁剪，只有调用时的注入范围会变化。
