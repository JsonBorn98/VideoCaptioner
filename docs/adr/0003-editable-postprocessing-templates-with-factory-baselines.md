# 使用可编辑后处理模板与不可变出厂基线

字幕后处理提供宽松、均衡和平滑优先三套完整模板，用户可以直接修改模板工作值，也可以从任一模板复制自定义方案。每个模板另有不可变出厂基线；模板恢复默认使用自身基线，自定义方案恢复默认使用其 `base_template_id` 对应的出厂基线，而不动态继承来源模板当前值。复制时仍复制来源模板当时的有效配置，因此用户能够以自己的模板调整作为新方案起点，同时避免来源模板后续修改悄然改变既有方案的复位结果。

后处理页面上的配置修改立即持久化到当前方案，不维护任务级临时覆盖。任务开始时冻结方案快照，从而保持交互简单，同时保证运行中修改不会改变已启动任务。
