---
status: accepted
---

# 连接测试与输出上限探查/能力测试的行为分叉

连接测试（任务前）只透传服务端真实报错文本，不做验证重试、不产生建议值；输出上限探查（编辑器内）才做解析+验证重试+应用。连接测试的目的是验证配置本身，降值重试会掩盖配置问题；且用户调整配置的入口在编辑器，任务前测试若自动降值会让「测试绿了、任务还是 400」的割裂。替代方案「连接测试也重试」被否决。
