---
status: accepted
---

# 后处理同轮修复采用冻结上下文与确定性归并

2026-09-09 性能治理 grilling 确认：后处理当前串行读取已被前批拆分修改的字幕状态，直接并发会使上下文版本、段索引和结果取决于完成顺序；改为同轮所有修复请求读取统一修复轮次快照，校对读取明确绑定的修复候选及版本化上下文，候选按固定顺序归并并重新验收，下一轮才使用新的字幕状态。

这一选择放弃同轮即时消费邻批最新修复的机会，换取可并发的请求、稳定的问题身份与可复现的归并语义；否决保留动态上下文并按邻居依赖串行调度。原文保护、必要校对、验收门槛、失败区域回退与主动停止不交付部分成果均不放宽；复用并发执行构件不代表复用上游翻译的 checkpoint 交付语义。
