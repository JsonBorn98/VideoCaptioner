# Archived Development Notes

This directory keeps development notes that no longer describe the current
project surface or that were produced as one-off research/design artifacts.

Archived on 2026-07-04:

- `api.md`: placeholder API note with stale `app.core` imports.
- `architecture.md`: old high-level sketch with stale `app/` module paths.
- `asr-chunk-merger.md`: old usage guide with stale imports and examples.
- `asr-chunked-usage.md`: old `ChunkedASR(base_asr)` guide; current API takes
  an ASR class plus `audio_path`.
- `asr-optimization-plan.md`: execution plan artifact.
- `asr-smart-boundary-chunking.md`: proposed design artifact, not current
  product documentation.
- `siliconflow-gemini-api-research.md`: dated provider research artifact.
- `translate-module.md`: old translation guide with stale `app.core` imports.
- `view-structure.md`: old UI directory sketch.
